import React from 'react';
import { AppWrapper } from '../../components';

import './profile.scss'
const Profile = () => {
    return (
        <div>Profile</div>
    )
}

export default AppWrapper(Profile)